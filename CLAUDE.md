# 若衆メンバー専用サイト — プロジェクト状況（2026-07-15時点）

このファイルは「今このプロジェクトが何であり、何が済んでいて、何が未確認か」を
会話の続きが無くても把握できるようにするための引き継ぎメモ。README.mdは利用者向け、
このファイルは開発を引き継ぐ側（Claude自身も含む）向け。

## プロジェクトの概要

- 若衆（伝統的な青年団）のメンバー専用サイト。GAS/スプレッドシート運用をやめて、
  素のHTML/CSS/JS + Supabase（Postgres, Auth, Storage, Edge Functions）で作った。
- ビルドツール無し。`index.html` を直接ブラウザで開くか、GitHub Pagesで配信。
- リポジトリ: https://github.com/wakasyu/member （public）
- 公開URL: https://wakasyu.github.io/member/
- Supabaseプロジェクトref: `szedtsppkoknipvsiekq`

## ファイル構成

| ファイル | 役割 |
|---|---|
| `index.html` | 画面本体（ログイン/トップ/予定一覧/日程調整回答/管理） |
| `styles.css` | デザイン。ロゴのブルーを基調にしたテーマ |
| `app.js` | 全ロジック（約1600行）。Supabase連携・描画・保存処理すべてここ |
| `config.js` | 実際のSupabase URL/anon key（公開前提のキーなのでコミット済みでOK） |
| `config.example.js` | ひな形 |
| `supabase-schema.sql` | テーブル・RLS・Storageバケット・Webhook基盤。**何度実行しても安全**（if not exists / if existsで書いてある） |
| `supabase/functions/notify-answer/index.ts` | 回答通知メール用Edge Function |
| `logo.jpg` | 実際のクラブロゴ（Instagramアイコン用に作られた正方形画像、丸枠で表示） |
| `README.md` | セットアップ手順・機能一覧（利用者向け） |

## できていること（機能一覧）

- ログイン（メール/パスワード）、パスワード再設定メール
- ログイン後の「トップ」ページ：Supabase Storageの非公開バケット
  （`top-photos`）から写真を取得し全画面背景でスライドショー表示。
  ヘッダー・タブ・タイトルは写真の上にガラス風パネルで重ねる。
  写真は管理画面「トップ写真」タブからアップロード/削除（管理者のみ書込み、
  ログイン済みなら誰でも閲覧）
- 予定一覧：日ごとに見出し、コンパクトな1行レイアウト（空の項目は非表示）、
  出欠人数はインラインバッジ、日程調整リンク/共有文コピーは見出し行の右側
  （狭い画面では自然に折り返す）
- 日程調整回答：ログイン中の本人として回答（名前選択は無し）。
  管理者だけ「代理入力するメンバー」を選んで他人の回答を編集可能。
  回答期限を過ぎると一般メンバーは編集不可（UIとRLS両方で強制）。
  回答の取り消し（未回答に戻す）も可能
- 管理画面：予定/メンバーのCRUD、削除は論理削除（予定は復元可）、
  分類・理由カテゴリの選択肢管理、操作ログ（実行者名も記録）、
  トップ写真管理、メンバー表示順の並び替え（▲▼ボタン）
- メンバー一覧・予定一覧の管理者用テーブルは幅700px以下でカード表示に自動切替
  （横スクロール不要）
- メール通知（Resend経由）：回答があるたびではなく、対象メンバー全員の
  回答が揃った時だけ管理者に通知
- リアルタイム反映（Supabase Realtime）
- メンバー自己登録：管理画面のメンバー一覧から「招待リンク」を発行すると、
  新メンバーはログイン不要でリンクを開き、自分のメール・電話番号・生年月日
  などを入力するだけで登録が完了する（Edge Function `register-member` が
  service_roleでSupabase Authアカウントを作成。初期パスワードは固定で
  「password」。ログイン後に本人が変更する運用）
- 候補日程調整（「候補日程調整」タブ）：特定の1予定の出欠（日程調整回答）とは
  別の仕組み。管理者が期間と1日の時間帯を決めて作成し、メンバーは
  Googleカレンダー風の週グリッドをドラッグして自分の空き時間を申告する。
  結果表示ではみんなの重なりをヒートマップ表示し、「空いている時間帯トップ3」
  「参加可能人数が多い日トップ3」を自動提案する。備考欄（1人1件、
  `availability_notes`）、管理者による代理入力（メンバー選択式）、
  自分の入力をリセットするボタンにも対応
- メンバー追加画面に「新規メンバー登録リンクを発行」ボタンがある。
  事前に名前などを入力しておく必要は無く、発行したリンクを開いた人が
  自分で名前・連絡先・メールアドレスを入力するとmembersテーブルの行
  そのものが新規作成される（`member_invites`テーブルでトークン管理。
  以前の「既存メンバー行にトークンを付与する」設計から作り直した）
- 予定一覧の各予定・各メンバーのところに参加/不参加/未定ボタンが直接ある
  （別画面に遷移せずその場で回答できる）。未定を押すと備考欄などが展開する。
  管理者は全メンバー分をその場で代理回答できる。自分の未回答には
  「要回答」バッジと左端のアクセント線で気付きやすくしてある
- 予定追加画面で対象メンバーを絞り込める（未指定なら在籍期間から自動判定、
  「入会者面談」等の一部メンバーだけが対象の予定にも対応）。
  絞り込んだメンバーごとにその場で出欠を事前入力することも可能
  （`event_target_members`テーブル。notify-answerの「全員回答完了」判定も
  この絞り込みを見るようにしてある）
- 予定の開始・終了時刻はネイティブの`<input type=time>`ではなく時/分の
  プルダウン2つに変更（時刻スピナーが59→00で無限ループする挙動を回避）
- 予定は同じ日付内では開始時刻順に並ぶ
- 候補日程調整のグリッドは1時間ごとの行にまとめ、15/30分刻みは行内の
  色分けブロックで表現（数字は出さない）。時刻ラベルは各行の境界線の
  すぐ横に配置し、境界線を太く色付けして横に長い週表示でも時間帯を
  追いやすくした。ドラッグ選択は即保存ではなく「完了」ボタンを押した
  時にまとめて保存する方式に変更（保存前は件数表示＋確認なしの週送り/
  ポール切替でも選択状態が消えない）
- ログイン画面に「スタッフとしてログイン」を追加。個別アカウントではなく
  スタッフ全員共通の1つのSupabaseアカウント（固定メールアドレス＋共通
  パスワード）でログインする方式。ログイン時に入力した名前をその
  セッション内だけ保持し、予定追加時の「作成者」に自動入力される。
  スタッフには「予定一覧」と新設の最上位タブ「予定追加」だけが見え、
  トップ・日程調整回答・候補日程調整・管理タブは非表示（`profiles.role`
  に`staff`を追加し、専用のRLSポリシーでイベントのinsertだけ許可）

## データベース・インフラの状態

- `supabase-schema.sql` は繰り返し実行安全だが、**このファイルを更新したら
  Supabase側で実際にSQL Editorで再実行してもらう必要がある**（自動デプロイの
  仕組みは無い）。直近の変更で `members.sort_order` 列を追加したが、
  ユーザーが実行済みか最終確認が取れていない（2026-07-13時点で一度
  "column does not exist" エラーが出て、実行を依頼した）。
- Storageバケット `top-photos`：非公開。RLSで「認証済みなら閲覧可、
  書き込みは管理者のみ」。
- Edge Function `notify-answer`：`supabase functions deploy notify-answer --no-verify-jwt`
  でデプロイ済み。secretsに `RESEND_API_KEY` / `ADMIN_NOTIFY_EMAIL` / `NOTIFY_FROM_EMAIL`
  / `WEBHOOK_SECRET` を設定済み。`answers`テーブルへのWebhookトリガー
  （`notify_answer_change`）も設定済み。
- Resendは無料枠でドメイン未認証のため、`ADMIN_NOTIFY_EMAIL`に設定した
  アカウント登録メール以外には送信できない。他の管理者にはGmailの
  転送フィルタで届くよう設定済み（サイト側の実装ではなく利用者側の設定）。
- Supabase Personal Access TokenやCLIログインは作業のたびに一時的に
  発行してもらい、作業後は失効させる運用にしている（常時保持しない）。

## 直近セッションで直した/作った主なもの（新しい順）

-1. （2026-07-15 続き）ユーザーからの要望11件に対応：
   - 回答区分に「時間限定」を追加（開始・終了時刻＋コメント）。
     `answers.limited_start_time`/`limited_end_time`列とstatusのcheck制約を更新
   - 予定一覧の回答チップの詳細欄（未定・不参加・時間限定）を
     `<details>`によるアコーディオン化（既存データがあれば自動展開）
   - 管理者向けに「編集/閲覧/メンバー」の表示モード切替を追加
     （画面右下に固定表示）。DBのroleやRLSは変えない表面的な切替のみ：
     閲覧モードは管理画面は使えるが他メンバーの出欠には触れない、
     メンバーモードは一般メンバーと同じ見た目になる
   - スタッフも予定追加時に対象メンバーの絞り込みができるように
     （事前代理回答は引き続き管理者のみ）
   - ホーム画面追加用アイコン（apple-touch-icon + manifest.json）を追加
   - ログイン画面の「パスワードをお忘れですか？」と
     「スタッフとしてログイン」の間に区切りを追加
   - スタッフ表示で意味のなかった「アカウント未紐付け」ピルを非表示に
   - 実装中に発見した実バグ修正：`groupEventsByMemberStatus`が
     ステータスをハードコードしていたため、STATUS_LISTに無い値の
     回答があると「人主体」表示がクラッシュする不具合を修正
   - 保留：「理由カテゴリが追加できない」という報告のうち、
     不参加時にカテゴリを選べない件は上記の対応で解消したはずだが、
     分類管理画面自体の「追加」操作の不具合は再現できておらず未解決
     （エラーメッセージの詳細待ち）

0. （2026-07-15）ユーザーからの要望10件に対応：予定一覧でのインライン回答、
   予定の対象メンバー絞り込み＋事前代理回答、時刻入力のプルダウン化、
   予定の時刻順ソート、日程調整回答画面のレイアウト改善、候補日程調整
   グリッドの1時間行化＋色分け表現、候補日程調整の「完了」ボタン化、
   スタッフ共通ログイン（予定一覧・予定追加のみ閲覧可）、袴サイズへの
   表記変更、トップ画面の要素整理。詳細は上の「できていること」を参照
1. ロゴの枠を角丸四角→円形に変更（正方形のInstagramアイコン画像の白い
   余白が見えていた問題）
2. パスワード再設定リンクが `localhost` に飛ぶ不具合 →
   Supabase側のSite URL設定が原因と特定、修正を依頼。コード側にも
   `onAuthStateChange`の`PASSWORD_RECOVERY`検知を保険で追加
3. メンバー表示順（`sort_order`）を管理画面から▲▼で変更できる機能を追加
4. 管理画面の予定一覧・メンバー一覧・ログをレスポンシブなカード表示に
   （700px以下で横スクロール解消）
5. トップ写真アップロード画面のスマホレイアウト崩れを修正
6. 実際のロゴ画像（`logo.jpg`）を反映
7. 予定一覧の1件あたりのレイアウトを大幅コンパクト化（空欄メタ情報を
   非表示、出欠人数をインラインバッジ化、ボタンを見出し行と同じ行に）
8. トップページを「写真を画面全体の固定背景にして全要素をその上に
   重ねる」構成に変更、ヘッダーに小さいブランドマーク＋青いアクセント
   ボーダーを追加
9. ユーザビリティレビュー（見やすさ/わかりやすさ/使いやすさ）を実施し
   12件の指摘に対応（詳細は git log 参照）
10. ログイン後のトップページに写真スライドショー＋次の予定/未回答数の
    ハイライトカードを新規実装（Supabase Storage経由、GitHub Pages上に
    静的ファイルで置くと非ログインでも見えてしまうため意図的にこの構成）
11. 管理者が他メンバーの回答を代理入力できる機能を追加（ログにも
    「誰が誰の回答を変えたか」を明記）
12. Resendメール通知（Edge Function + Database Webhook）、回答期限を
    過ぎたら一般メンバーは編集不可（RLSでも強制）、メンバー項目の拡充
    （表示名・生年月日・Tシャツサイズ・担当）、電話番号のハイフン必須化 など
13. セキュリティ監査で見つけた重大なRLS不備（一般メンバーが自分を
    admin に昇格できた）を修正。ブランドカラーをロゴのブルーに刷新

## 未確認・保留中の項目

- **今回追加した`answers.limited_start_time`/`limited_end_time`列と
  statusのcheck制約（'時間限定'追加）を反映するため、
  `supabase-schema.sql`の再実行がまだ必要**（下の項目と合わせて1回で良い）
- 「理由カテゴリが追加できない」という報告の原因が特定できていない。
  コード上は正常に見えるため、実際のエラーメッセージ・画面が必要
- **`supabase-schema.sql`をSQL Editorで再実行する必要がある**（今回のセッションで
  `event_target_members`テーブル、`profiles.role`の`staff`追加、
  `is_staff()`、スタッフ用の予定insertポリシー等を追加した）
- **`notify-answer`のEdge Functionを再デプロイする必要がある**（対象メンバーの
  絞り込み(`event_target_members`)を見るように変更したため）：
  `supabase functions deploy notify-answer --no-verify-jwt`
- **スタッフ用の共通Supabaseアカウントをまだ作成していない**。ダッシュボードの
  Authentication > Users から手動で1つ作成する必要がある：
  - メールアドレスは `app.js` 内の `STAFF_LOGIN_EMAIL`（現在
    `staff@wakasyu.local`）と完全一致させる
  - パスワードがそのままスタッフ全員の共通ログインパスワードになる
  - 作成後、SQL Editorで
    `insert into public.profiles (id, display_name, role) values ('（作成したユーザーのUUID）', 'スタッフ', 'staff');`
    を実行してrole紐付けが必要（手順はREADME.mdの管理者作成手順と同様）
- メンバー自己登録の初期パスワードは全員共通で固定文字列「password」。
  本人がログイン後すぐに変更する前提の運用のため、招待リンクの管理
  （誰にいつ送ったか）は引き続き重要
- 候補日程調整は週表示のみで月表示は無い（期間内を週送りする形）
- Resendでドメイン認証していないため、管理者以外への直接メール送信は
  非対応のまま（Gmail転送で代替済み、利用者はこれで良いと合意済み）
- メンバーのログインアカウント発行は、通常メンバーは招待リンクで
  セルフサービス化済み。管理者アカウントの追加だけは今もダッシュボードから
  の手動作業のまま

## 作業の進め方（このプロジェクトでの申し合わせ）

- 変更は都度 `git commit` → `git push` している（GitHub CLIでブラウザ認証、
  トークンは使い終わったら失効させる運用）
- SupabaseのSQL/Storage/Edge Functionをこちらから直接操作する場合は、
  ユーザーにPersonal Access Tokenを都度発行してもらい、作業後は
  ダッシュボードから削除してもらっている
- 見た目の変更は可能な範囲でこのブラウザプレビュー環境で確認してから
  pushしているが、この環境のスクリーンショット機能は時々タイムアウト/
  幅の大きいウィンドウを正しく撮れないことがある。その場合は
  `getComputedStyle`や`getBoundingClientRect`などJS経由の数値検証で
  代替している
- ユーザーは「プロンプトを作って実行して」という形で、要件を明確化した
  上で実装からGitHubへの反映まで一気に任せるスタイルを好む
