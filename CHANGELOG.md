# 若衆メンバー専用サイト — 変更まとめ（〜2026-08-12）

このファイルは、開発の経緯を時系列で振り返れるようにするための記録。
「今のルール」はSPEC.md、実装の詳細・ハマりどころ・未確認事項はCLAUDE.mdを参照。
このファイルは過去を振り返るための記録であり、以後の変更履歴は基本的に`git log`を見ればよい
（大きな区切りがあれば追記する）。

---

## 初期構築（〜2026-07-23）

GAS/スプレッドシート運用をやめ、素のHTML/CSS/JS + Supabaseで作り直した最初の期間。

- ログイン（メール/パスワード）、メンバー自己登録（招待リンク）、予定のCRUD、
  出欠回答（その場回答＋専用画面）、管理者の代理回答、操作ログ、分類・理由カテゴリ管理
- スタッフ共通ログイン（固定パスワード、予定一覧・予定追加のみ）を追加
- 管理者の「編集/閲覧/メンバー」表示モード切替（DBの権限は変えず見た目だけ）
- セキュリティ・a11y・UXの一括強化（初期パスワードのランダム化、招待トークンの
  排他制御、XSS対策、CDNスクリプトのSRI固定、キーボード操作対応など）
- 日程アンケート（`availability_polls`）を新設し、期間＋共通時間帯モデルから、
  候補日ごとに個別の時間帯を持てるモデルへ再設計。Googleカレンダー風の月表示
  カレンダーでの候補日複数選択＋スワイプでの時間帯指定に作り直した
- 回答期限（予定・日程アンケート共通）と、期限前日リマインドメール
  （`deadline-reminder`、pg_cron）を追加
- 「研修」ロールを追加（権限はmemberと同じ、表示ラベルのみ区別）
- 日程アンケートの「全員回答完了」通知、未回答メンバー一覧表示を追加

---

## 2026-08-03：SPEC.mdの新設

若衆専用サイトの仕様ルールを決める土台として、それまでのコンテキストを
SPEC.md（仕様書）にまとめた。CLAUDE.md（開発引き継ぎメモ）とは別に、
「今のルール」だけを一覧できる場所を作った。

## 2026-08-06：使い方ページの追加

サイト上部タブに「使い方」を新設。予定一覧・出欠回答・日程アンケートの
使い方に加え、運用ルール（調整系の回答後は一報、など）を掲載。
GitHub Pagesのビルドが一時的に失敗する問題（Jekyll処理が素のJS/HTMLサイトに
不要な悪さをしていた）に気づき、`.nojekyll`を追加して解消。
同時期にGitHubの障害でPagesのビルドが詰まる事象にも遭遇し、空コミットで
再キックして復旧を確認した。

## 2026-08-10：招待リンク・在籍状態・使い方ページの改修

- **招待リンクを「1回限り」から「無効化するまで何度でも使える」方式に変更**。
  1つのリンクをLINEグループへまとめて配って複数人に登録してもらう運用に
  合わせるため（以前の方式だと2人目以降が必ず失敗していた）。移行時点の
  既存リンクは一括無効化し、管理画面に発行済みリンク一覧＋無効化ボタンを追加
- 電話番号入力のハイフンを必須ではなくした（スマホの電話用キーパッドは
  ハイフンキーが押しにくいため）
- 在籍状態（`member_state`）に「研修」を追加し、管理画面から直接設定できるように
  （以前はSQLでの手動変更が必要だった）。あわせて「学年」入力欄を廃止
- 使い方ページの内容をロールごとに出し分け（スタッフには実際に使う
  「予定一覧」「予定追加」の説明だけを表示）

## 2026-08-11：登録トラブルの調査と初期パスワードの見直し

実際に登録者が「load failed」で登録できず、名前・メールアドレスも
「登録済み」扱いになって詰む事例が発生。調査の結果、以下の2つの根本原因が
見つかり、どちらも修正した。

1. **初期パスワードのランダム発行方式に構造的な弱点があった**：生成した
   パスワードを本人に渡す経路が「登録直後の画面」と「メール」の2つしか無く、
   両方失敗すると本人が完全にパスワードを知りようがなくなる。
   → **固定値「wakashu2026」に戻した**（ログイン直後に強制パスワード変更
   画面へ誘導する仕組みは維持）
2. **ウェルカムメール送信（Gmail SMTP、2〜3秒かかる）を待ってから応答を
   返していたため、ブラウザのfetchがタイムアウトして「Failed to fetch」に
   なっていた**（サーバー側の登録処理自体は毎回成功していた）。
   診断用の一時的なEdge Functionを作って`curl`とブラウザ`fetch`を比較し
   特定。→ メール送信を`EdgeRuntime.waitUntil()`でバックグラウンド化し、
   応答時間を3〜5秒から0.5〜0.6秒に短縮

## 2026-08-12：スタッフ権限の拡張、予定の非表示機能、使い方ページの調整

- **政やスタッフに「メンバー一覧」（閲覧のみ）「日程アンケート」（作成・編集・
  削除）を開放**。当初は既存の「管理」タブのサブタブとして開放したが、
  「管理タブ自体は要らない、中身を直接タブとして出したい」とのフィードバックを
  受けて、独立した2つのタブとして直接開く形に変更した
- 日程アンケートの作成・編集を保存した直後、回答リンクを含む共有文を
  自動でクリップボードにコピーするように変更（管理者・スタッフ共通）
- 使い方ページの文言を継続的に調整（見出しの表現、注意事項の文面、
  スタッフ向けの補足など）
- UIの細かい改善2件：
  - 予定編集の対象メンバー一覧で、チェックを外した行が薄くグレーアウトして
    「選べないように見える」問題を解消（他の行と同じ見た目に統一）
  - 使い方ページの「締切後の連絡フロー」を、縦に3行並べる書き方から
    横並びの矢印つきピル表示（①→②→③）に変更し、縦に間延びしないように
    整理
- **予定の「非表示」機能を新規追加**：削除とは別軸で、予定ごとに一般メンバー
  向けの表示（予定一覧・トップの次の予定・出欠回答の選択肢）からだけ外せる
  トグルを追加。個別のオン/オフに加えて、「終了した予定をまとめて非表示に
  する」一括操作ボタンも追加した

---

## 現在のオープンな論点

SPEC.md「12. 今後決めるべき事項」を参照（研修ロールの権限の細分化、
招待リンクでの研修者直接登録、日程アンケートの未回答判定の限界）。
