# 若衆メンバー専用サイト — 仕様書（2026-08-06時点）

このファイルは、今このサイトが「何を・どういうルールで」動かしているかを
一覧できるようにするための仕様書。今後の仕様変更を検討するときの土台として使う。

- 実装の詳細・作業の申し合わせ・未確認事項は [CLAUDE.md](CLAUDE.md) を参照
- セットアップ手順は [README.md](README.md) を参照
- このファイルは「今のルール」を記述する場所。変更履歴は`git log`を参照

---

## 1. サイトの目的

若衆（伝統的な青年団）のメンバー専用サイト。GAS/スプレッドシート運用の代わりに、
予定の出欠管理・日程調整・メンバー名簿を一元化する。

- 公開URL：https://wakasyu.github.io/member/
- リポジトリ：https://github.com/wakasyu/member（public）
- 技術構成：素のHTML/CSS/JS（ビルド無し）+ Supabase（Postgres/Auth/Storage/Edge Functions）

---

## 2. ロールと権限（4種類）

| ロール | ログイン方法 | 見える範囲・できること |
|---|---|---|
| **admin（管理者）** | 個別メール/パスワード | 全操作可能。画面右下の「編集/閲覧/メンバー」表示モード切替で、DBの権限は変えずに見た目・操作範囲だけ切り替えられる（閲覧＝管理画面は使えるが他メンバーの出欠には触れない／メンバー＝一般メンバーと同じ見た目） |
| **member（メンバー）** | 個別メール/パスワード | 自分の出欠回答・日程アンケートへの入力ができる。他メンバーの代理はできない |
| **staff（政やスタッフ）** | スタッフ共通の1アカウント（固定メール＋共通パスワード） | 「予定一覧」「予定追加」タブのみ表示。ログイン時に入力した名前がセッション中「作成者」欄に自動入力される |
| **研修** | 個別メール/パスワード（2026-07-23時点で未発行） | **権限はmemberと完全に同じ**。ヘッダーの表示ラベルだけ「研修」になる。入会前・研修期間中の人向けの区分として用意。招待リンク経由でmember登録された後、必要になったら管理者がSQLで`研修`に変更する運用 |

- ロールの判定はサーバー側`profiles.role`が正。RLSは`is_admin()`/`is_staff()`関数で判定し、
  どちらにも該当しなければ全てmember相当の権限になる（研修もこの分岐に入る）
- 管理者アカウントの追加・スタッフ共通アカウントの作成・研修へのロール変更は、
  今のところアプリのUIには無く、Supabaseダッシュボード/SQL Editorでの手動作業

---

## 3. メンバー管理

- メンバー情報：氏名・表示名（略称、任意）・学年・年齢・連絡先（電話番号）・
  在籍状態（在籍/休会/退会）・在籍期間（入会日/退会日）・衣装サイズ・カバンサイズ・備考
- 同姓同名は登録不可（DBレベルでユニーク制約）
- **メンバー自己登録**：管理者が発行した招待リンク（`?register=token`）を開くと、
  ログイン不要で名前・連絡先・メールアドレス等を入力するだけで`members`行と
  Supabase Authアカウントが両方作られる。ロールは**常に`member`固定**で作られる
  （研修者として登録したい場合は、登録完了後に管理者が手動で`研修`に変更する）
- 招待リンクは1回限り有効（使用済みなら再利用不可、排他制御あり）
- 初期パスワードは登録の都度ランダム発行し、登録直後の画面とメールにのみ表示。
  ログイン時`must_change_password`フラグが立っていると強制的にパスワード変更画面へ
- **メンバー削除**：`members`行を削除すると、紐づくSupabase Authアカウントも
  自動的に削除される（`delete-member` Edge Function）。これにより同じメールアドレスで
  再登録できるようになる
- 対象メンバーの自動判定（予定・出欠期限判定などで使用）：`join_date`〜`leave_date`の
  期間で判定。`event_target_members`に手動指定があれば自動判定より優先される

---

## 4. 予定（events）

### 4.1 基本項目
名称・分類（管理者が選択肢を追加/名称変更可能）・日付・開始時刻/終了時刻・場所・
場所URL・作成者・回答期限（任意）・備考

### 4.2 対象メンバー
- 既定：在籍期間（`join_date`〜`leave_date`）で自動判定
- 個別に絞り込みたい場合：`event_target_members`に手動登録したメンバーだけが対象になる
  （1件でも登録があれば自動判定より優先）

### 4.3 出欠回答
- ステータス：`参加` / `不参加` / `未定` / `時間限定` / `未回答`
- `時間限定`のときは開始・終了時刻を追加入力できる
- `不参加`/`未定`のときは理由カテゴリ（管理者が選択肢管理）＋自由記述を追加入力できる
- `未定`は「いつまでに確定するか」の予定日も入力できる
- 回答経路は2つ：①予定一覧でその場回答（バッチ保存方式：保留→「回答完了」ボタンで
  まとめてDB反映）②出欠回答画面（招待リンク`?schedule=token`経由、代理入力もここ）
- 管理者は全メンバー分をその場で代理回答できる（代理変更中は注意書きを表示）

### 4.4 回答期限
- `answer_deadline`（日付のみ、任意）を過ぎると**一般メンバーは回答の追加・変更・削除ができなくなる**
  （UI側の無効化＋DB側のRLSの両方で強制）
- 締切日当日は回答可能（23:59まで）。締切日の翌日からロックされる
- 管理者（表示モードが「編集」）は期限後も編集可能

### 4.5 通知メール
- **回答が全員分そろった時**：対象メンバー全員の回答が揃った瞬間に1回だけ、
  管理者（`ADMIN_NOTIFY_EMAIL`）へ通知（`notify-answer` Edge Function、
  `answers`テーブルのinsert/updateをトリガーに起動）
- **回答期限の前日**：期限が明日になった予定のうち、未回答のメンバーがいれば、
  予定名・未回答者名を管理者へ通知（`deadline-reminder` Edge Function、
  pg_cronで毎日8:00 JST起動）。メンバー本人への個別送信ではない

---

## 5. 日程アンケート（availability_polls）

特定の1予定の出欠とは別の仕組み。予定を立てる前段階として、複数の候補日それぞれの
空き時間をメンバー同士で申告し合う。

### 5.1 候補日の作成（管理者）
- 月表示カレンダー（Googleカレンダー風）から候補日を複数タップで選ぶ（**連続していなくてよい**）
- 「完了」を押すと、選んだ候補日それぞれについて時間帯をスワイプ（ドラッグ）で指定する
  画面に進む。候補日ごとに個別の時間帯を設定できる
- 時間の単位（15分/30分/60分）はアンケート全体で共通
- 回答期限（任意）を設定できる

### 5.2 メンバーの回答
- Googleカレンダー風のグリッド（候補日×時間）をドラッグして空き時間を申告する
- 「入力開始」を押すまではグリッドがロックされ、誤スワイプでの意図しない選択を防ぐ
  （ロック中でも、すでに自分が選択済みの時間帯は色付きのまま見える）
- 備考欄（自由記述）でも回答できる（例：「後半なら参加できるかも」）
- 管理者は他メンバーの代理入力ができる
- 「みんなの結果を見る」でヒートマップ表示＋「空いている時間帯トップ3」
  「参加可能人数が多い日トップ3」を自動提案。未回答のメンバー一覧も表示

### 5.3 未回答の判定ルール
日程アンケートには「対象メンバー」を絞る仕組みが無いため、**在籍中の全メンバー**が対象。
未回答の判定は「空き時間（`availability_slots`）も備考（`availability_notes`）も
どちらも1件も無い」こと。空き時間0件そのものが「その期間は全部埋まっている」という
意思表示である可能性と区別できないため、**備考だけ書いてあっても回答済み扱い**にする
（この割り切りは全ての未回答判定ロジック—トップ画面表示・期限リマインド・完了通知—で共通）。

### 5.4 回答期限
- 予定と同じルール：期限日を過ぎると一般メンバーは入力・変更ができなくなる
  （管理者は表示モードが「編集」なら期限後も編集可能）
- 期限が無いアンケートはいつまでも回答可能

### 5.5 通知メール
- **回答が全員分そろった時**：予定と同じ`notify-answer`関数で処理
  （`availability_slots`/`availability_notes`のinsert/updateをトリガーに起動）
- **回答期限の前日**：予定と同じ`deadline-reminder`関数で処理。
  1通のメールに【予定】【日程アンケート】のセクションとしてまとめて届く

---

## 6. 通知メールの一覧

| 種類 | 宛先 | タイミング | 二重送信防止 |
|---|---|---|---|
| メンバー登録完了 | 登録した本人 | 自己登録の直後 | （1回限りの操作なので不要） |
| 出欠回答が全員分そろった | 管理者 | 予定の対象者全員が回答した瞬間 | `events.completion_notified_at` |
| 日程アンケートが全員分そろった | 管理者 | 在籍中の全メンバーが回答した瞬間 | `availability_polls.completion_notified_at` |
| 回答期限が明日 | 管理者 | 毎日8:00 JST（該当があれば） | `events.reminder_sent_at` / `availability_polls.reminder_sent_at` |

- 全て管理者向けで、未回答メンバー本人への個別催促メールは無い
  （管理者が一覧を見て声かけする前提の設計）
- 送信元：完了系（全員そろった/期限リマインド）はResend、登録完了メールのみGmail SMTP
  （Resendは送信ドメイン未認証のため、`ADMIN_NOTIFY_EMAIL`宛て以外には送れない制約があるため）

---

## 7. 管理機能

- 予定・メンバーのCRUD（予定の削除は論理削除で復元可）
- 分類・理由カテゴリの選択肢管理（追加・名称変更）
- 操作ログ（誰が・何を・いつ変更したか）
- トップ写真管理（Storageの非公開バケット、ログイン必須）
- メンバー表示順の並び替え
- メンバー削除（Supabase Authアカウントも連動削除）
- 日程アンケートの作成・編集・削除（論理削除、メンバーの入力データは残る）

---

## 8. UI/UX方針

- トップページ：写真スライドショー＋「次の予定」「あなたの回答：未回答◯件」
  （予定と日程アンケートの未回答を合算表示。クリックで該当画面へ）
- 予定一覧：日ごと見出し、分類ごとに色分けした帯、既定は折りたたみ・要回答の予定だけ自動展開
- 管理者用テーブルは幅700px以下でカード表示に自動切替
- レスポンシブ：スマホでは画面いっぱい、PC（701px以上）ではカレンダー等の
  大きくなりすぎる要素を縮小
- 右下固定UI（表示モード切替・回答完了バー）は他のUIと重ならないよう自動で余白確保

---

## 9. インフラ・技術構成

- ビルド無し。`index.html`を直接開くかGitHub Pagesで配信
- Supabaseプロジェクトref：`szedtsppkoknipvsiekq`
- Edge Functions：
  - `register-member`（メンバー自己登録、`--no-verify-jwt`）
  - `delete-member`（メンバー削除時のAuthアカウント削除、JWT検証あり）
  - `notify-answer`（全員回答完了通知、Database Webhookから起動）
  - `deadline-reminder`（期限前日リマインド、pg_cronから毎日起動）
- RLS（Row Level Security）：全テーブルで有効。「本人の分のみ書き込み可、
  管理者は全て可、期限後は管理者のみ」という形を一貫して適用

---

## 10. 使い方ページ（guideView）

サイト上部タブ「使い方」から、全ロール共通で見られる静的な説明ページ。
サイトの機能説明だけでなく、若衆としての運用ルールもここに明記している。

### 10.1 掲載内容
- 予定一覧・出欠回答・日程アンケート、各画面の使い方
- 「使ううえで気をつけてほしいこと」（次章11の内容と同一文面）

---

## 11. 利用上の留意点（運用ルール）

サイトの機能とは別に、メンバーが守るべき運用上のルール。使い方ページ（10章）に
同じ内容を掲載している。ここが正であり、ルールを変える時はSPEC.mdと
使い方ページ（`index.html`の`#guideView`）を両方更新する。

- **調整系の回答後は一報**：出欠回答・日程アンケートに入力しただけでは
  気づいてもらえないことがあるため、回答後はLINEグループにも一言報告する
- **「未定」はいつまでに分かるか一報**：サイト上の「いつまでに分かるか」欄への
  入力だけでなく、いつ頃分かりそうかをLINEグループにも一報する
- **自分で決めた報告期日は守る**：「未定」のときに自分で設定した
  「いつまでに分かるか」の期日は必ず守り、期日までに参加／不参加を確定させて
  回答を更新する
- **締切を過ぎて予定が変わる場合は若衆と政やに一報**：回答期限を過ぎるとサイト上
  では自分で変更できなくなるため、サイトでの修正を待たず若衆メンバーと政や
  （スタッフ）の両方にLINE等で一報する
- **締切後の代理変更が終わったら一報＆本人確認**：締切後の変更は管理者による
  代理入力で対応するため、変更後はその旨を一報するとともに、本人にもサイト上の
  内容が合っているか確認を取る

---

## 12. 今後決めるべき事項（オープンな論点）

- **研修ロールの権限**：現在はmemberと全く同じ。制限が必要になったら都度検討
  （例：メンバー一覧の閲覧範囲、出欠回答の可否など）
- **招待リンクで研修者を直接登録する経路**：現状は登録後に手動でロール変更する運用。
  頻度が増えるなら招待リンク自体に「研修として登録」オプションを追加するか検討
- **日程アンケートの未回答判定**：「空き時間0件」と「まだ手つかず」を区別できない
  現在のルールで運用上問題が出るようなら、専用の「回答済みフラグ」の追加を検討
